import React from 'react';

const CommunityThreads: React.FC = () => {
  return (
    <div className="community-threads">
      <p>Discussion threads (e.g., "Ask a local guide" or "Share your travel story") will be implemented here.</p>
    </div>
  );
};

export default CommunityThreads;